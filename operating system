#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h> 
#include <sys/wait.h>
int main (void) 

 {
  printf("Child process => PPID=%d,PID=%d\n",getpid(),getpid());
 exit (EXIT_SUCCESS);
}
else if (pid>o)
                {
                        printf("parent process=> PID=%d\n",getpid());
                        printf("waiting for child process to finist /n");
                        wait(NULL);
                        printf("child process finished \n");
                }
else {
    printf ("Unable to create child Process \n");
}
return EXIT_SUCCESS;
